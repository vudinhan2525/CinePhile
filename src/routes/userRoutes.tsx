import { RouteObject } from "react-router-dom";

const userRoutes: RouteObject[] = [
  // {
  //   path: "/booking/:showtimeId",
  //   element: <ProtectedRoute role="user"><BookingPage /></ProtectedRoute>,
  // },
];

export default userRoutes;